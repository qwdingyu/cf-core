# 快速开始

## 在新项目中使用

### 1. 添加依赖

```json
{
  "dependencies": {
    "@iusethink/cf-core": "file:../../packages/cf-core"
  }
}
```

### 2. 配置 tsconfig.json

```json
{
  "compilerOptions": {
    "paths": {
      "@iusethink/cf-core/*": ["../../packages/cf-core/src/*"]
    }
  }
}
```

### 3. 最小入口

```ts
// src/index.ts
import { bootstrap } from "@iusethink/cf-core/bootstrap";
import * as schema from "./db/schema";

export default bootstrap({
  schema,
  registerRoutes: (api) => {
    api.get("/hello", (c) => c.json({ ok: true, message: "Hello!" }));
  },
  pageRoutes: { "/admin": "/admin.html" },
});
```

### 4. 数据库 Schema

```ts
// src/db/schema.ts
export { systemConfig, adminAuditLogs, apiKeys } from "@iusethink/cf-core/db/schema";

import { sqliteTable, text, integer } from "drizzle-orm/sqlite-core";

export const products = sqliteTable("products", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  priceCents: integer("price_cents").notNull(),
});
```

## 按需引入功能模块

| 需要什么 | 导入路径 |
|---------|---------|
| 支付（支付宝） | `@iusethink/cf-core/features/payment` |
| 邮件（Resend） | `@iusethink/cf-core/features/email` |
| Webhook 通知 | `@iusethink/cf-core/features/webhook` |
| 智能路由 | `@iusethink/cf-core/features/thompson-router` |
| 防刷检测 | `@iusethink/cf-core/features/anti-abuse` |
| Telegram 集成 | `@iusethink/cf-core/features/telegram-miniapp` |

## 运行测试

```bash
cd packages/cf-core
npm test
```
